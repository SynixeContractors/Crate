{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "NATS Port-forward",
            "type": "shell",
            "command": "kubectl ctx do-tor1-dev && kubectl port-forward -n synixe service/synixe-nats 4222",
            "presentation": {
                "reveal": "always",
                "panel": "new"
            }
        }
    ]
}
