[package]
name = "synixe-model"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = { workspace = true, optional = true, features = ["serde"] }
serde = { workspace = true, features = ["derive"] }
sqlx = { workspace = true, optional = true }
uuid = { workspace = true, optional = true, features = ["serde"] }
serenity = { workspace = true, features = ["utils", "builder"], optional = true }

[features]
default = [
    "missions",
    "roles",
]
certifications = [ "uuid", "roles", "chrono" ]
mission-schedule = [ "missions", "chrono", "uuid" ]
missions = []
roles = [ "serenity" ]
